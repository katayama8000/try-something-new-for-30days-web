import { DefaultTemplate } from '../../templates/defaultTemplate';
import { type NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <DefaultTemplate>
      <h1>hello</h1>
    </DefaultTemplate>
  );
};

export default Home;
