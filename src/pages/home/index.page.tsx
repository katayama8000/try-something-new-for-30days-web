import { type NextPage } from 'next';

import { DefaultTemplate } from '../../templates/defaultTemplate';

const Home: NextPage = () => {
  return (
    <DefaultTemplate>
      <h1>hello</h1>
    </DefaultTemplate>
  );
};

export default Home;
